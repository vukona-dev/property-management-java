# property-management-java
A Java-based property management system with loan calculation, commission tracking, and GUI interface. Implements a three-tier architecture (PD, DA, GUI) with object stream persistence, custom exceptions, and Swing-based forms for managing sold properties.
